{
  "properties": [
    {
      "name": "ignite.persistence.enabled",
      "type": "java.lang.Boolean",
      "description": "Defines whether Apache Ignite will persist the cache."
    },
    {
      "name": "ignite.persistence.directory",
      "type": "java.lang.String",
      "description": "Defines the directory where Apache Ignite will persist data. This setting only applies if persistence is enabled."
    }
  ]
}