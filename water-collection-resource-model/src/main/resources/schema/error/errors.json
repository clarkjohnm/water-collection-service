{
  "$schema": "http://json-schema.org/draft-04/hyper-schema#",
  "title": "Error",
  "description": "REST Errors",
  "type": "object",
  "properties": {
    "errors": {
      "description": "An array of error objects {read only}",
      "readOnly": true,
      "type": "array",
      "items": {
        "$ref": "error.json"
      }
    }
  },
  "required": [
    "errors"
  ]
}